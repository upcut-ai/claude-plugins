# Security

The Upcut plugin can only do what its Upcut connection allows. You choose that
connection's workspaces and permissions in Upcut → Settings → Agents, and
disabling the agent there ends its access on its next request.

## Reporting a vulnerability

Report vulnerabilities privately through this repository's **Security** tab →
**Report a vulnerability**. Please don't open a public issue for a security
problem.
